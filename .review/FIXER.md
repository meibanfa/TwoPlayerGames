# Independent Fixer Instructions

You are a separate, fresh Codex implementation session. Read `AGENTS.md`, the repository, and `.review/latest.json`. Treat review findings as hypotheses: verify each against the actual code before changing anything. Fix well-supported P0/P1/P2 findings, add or update focused regression tests, and run relevant validation. If a finding is incorrect, do not damage working code; document why it is invalid in your final response.

Work only in the existing milestone branch. Do not reset, discard unrelated changes, push, merge, add unrelated features, or rewrite history. Verify each accepted finding, make the narrow fix and regression test, and record the completed fix in a focused local commit. The parent workflow must keep the original milestone base for verification and every fresh review round; it owns the single post-PASS branch push and pull request. Preserve the Chinese UI, extensible registry, real room/WebSocket/reconnect flow, and server-authoritative hidden-information boundary. Never expose opponent mine coordinates. Finish with a concise summary of changes, tests, commit, and any rejected findings.
